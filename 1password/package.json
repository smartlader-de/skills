{
  "name": "1password",
  "version": "2.0.0",
  "private": true,
  "description": "1Password environments and SSH/Git skills for Claude Code, Codex, and Gemini CLI",
  "scripts": {
    "test": "bash tests/entrypoints.test.sh && bash tests/skill-paths.test.sh && node --test tests/parse-dotenv.test.js tests/compare-env-names.test.js tests/check-account-binding.test.js && bash tests/redact-output.test.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
