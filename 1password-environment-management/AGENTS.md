## 1Password Skills

Two skills for securely managing credentials through 1Password.

Invoke via skill name:
- `1password:environments` — secrets, environment variables, provider sync
- `1password:ssh-git` — SSH key generation, provider registration, Git signing, server access

See CLAUDE.md for trigger phrases and descriptions.
